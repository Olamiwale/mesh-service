name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  
jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Point to minikube docker daemon
      run: eval $(minikube -p minikube docker-env)

    - name: Build product service Docker image
      run: |
            cd app/go-services/product
            docker build -t product-service .
            


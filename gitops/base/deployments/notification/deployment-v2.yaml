apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-deployment-v2
  namespace: k8s
  labels:
    app: notification-service
    version: v2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
      version: v2
  template:
    metadata:
      labels:
        app: notification-service
        version: v2
    spec:
      containers:
        - name: notification-service
          image: briitzacr.azurecr.io/notification-service:v2
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
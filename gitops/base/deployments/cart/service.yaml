apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: k8s
spec: 
  selector:
    app: cart-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
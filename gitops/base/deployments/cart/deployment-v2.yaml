apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-deployment-v2
  namespace: k8s
  labels:
    app: cart-service
    version: v2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart-service
      version: v2
  template:
    metadata:
      labels:
        app: cart-service
        version: v2
    spec:
      containers:
        - name: cart-service
          image: briitzacr.azurecr.io/cart-service:v2
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"